services:
  navidrome:
    image: deluan/navidrome:latest
    userns_mode: keep-id
    ports:
      - "4533:4533"
    restart: unless-stopped
    environment:
      ND_BASEURL: https://music.lab.rdrachmanto.dev
    volumes:
      - "./data:/data"
      - "/mnt/music:/music:ro"
